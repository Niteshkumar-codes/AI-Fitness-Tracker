# Constants Directory

Stores read-only global configurations and static metadata:

- **`config.js`**: Backend base API URLs, application variables, and local storage keys.
- **`enums.js`**: Key mapping lists (e.g. meal types, workout types, goal options) to prevent magic string typos in forms and APIs.
