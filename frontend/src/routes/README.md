# Routes Directory

Configures the React Router navigation routing for the application:

- **`AppRoutes.jsx`**: Central Router setup containing public, guest-only (e.g. login), and private paths (secured by checking authentication state).
