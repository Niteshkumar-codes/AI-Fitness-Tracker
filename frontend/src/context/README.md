# Context Directory

Contains global React Context providers for application state management:

- **`AuthContext.jsx`**: Global authentication state management (login status, JWT storage, user profile state).
- **`ThemeContext.jsx`**: Light/Dark theme configuration context.
