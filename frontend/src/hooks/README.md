# Custom Hooks Directory

Contains reusable React hooks to share stateful logic across multiple components:

- **`useAuth.js`**: Shortcut hook to easily fetch AuthContext details.
- **`useFetch.js`**: Generalized hook to handle loading state, response caching, and error handling for standard API requests.
